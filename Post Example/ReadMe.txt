Simple Node JS Post example.

To set up, first create Request Bin at http://requestb.in. 
Once you have a request bin id, swap this into the post.js file below. 
Then run
	node post.js

Return to http://requestb.in to see the posted data.